# Письмо

`Привет, бро!`

    И вновь, мне нужна твоя помощь. Как ты , возможно, знаешь, я работаю над системой управления пиццерией. 
    
Сейчас мне нужно срочно переключиться на другой проект, но и тут нужно довести дело до конца. Сроки!

Мне необходимо реализовать сохранение данных в отдельный `Json` файл. Я это сделал. Файл называется `dump.json`
Данные в файл попадают, однако секция **receipt_model** пуста. В ней нет данных хотя все тесты "зеленные".


```json
  "receipt_model": [
        {
            "data": null
        }
    ],

```
**Я не могу найти проблему** и рассчитываю на твою помощь. Мне нужно сделать корректное сохранение данных в файл и загрузку. Добавить в settings.json флаг `first_start`
Так же, нужно привести к единому формату файл `default.json` и `dump.json`
Нужно чтобы загрузка данных осуществлялась одинаково. Единый механизмом. При изменении данных, изменения должны попадать в файл `dump.json`

Еще я прошу тебя запустить и добавить RestApi. Нужно добавить универсальный метод для получения любого справочника в `Json` формате (GET) 
и изменения данных любого справочника (POST) в том числе и добавление нового рецепта.

А так же, нужно добавить GET метод который будет возвращать текущие остатки на **указанную дату**. Остатки так же нужно сохранить в файле `dump.json` и использовать их `сохраненные остатки` при расчете учитывания `blocked_period` 

### Итого
- Разобраться с некорретной выгрузкой данных в файл `dump.json`
- Дописать код и сделать унифицированный вариант загрузки / выгрузки данных из файлов: `dump.json`, `default.json`
- Добавить в RestApi GET запрос для получения данных по справочникам / транзакциям / остаткам
- Добавить в RestApi POST запрос для записи данных.
- Обязатиельно поверить и сделать тесты
- Добавить в RestApi POST запрос для изменения даты блокировки и записи в `dump.json`

Выручай, спасибо.




